const path = require('path');

/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  turbopack: {
    root: 'C:/Users/igor_/lasy-apps/Portal BIG',
  },
}

module.exports = nextConfig
